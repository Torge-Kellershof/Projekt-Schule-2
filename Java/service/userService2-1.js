class UserService {

//constructor(){}

static displayUser(user)        {
  return "<tr><td>" + user.id + "</tr></td>"
//  return "displayUser";     }


static changePassword(oldPassword, newPassword, repeatenewPassword){
  return "passwordChanged";                                         }
}
